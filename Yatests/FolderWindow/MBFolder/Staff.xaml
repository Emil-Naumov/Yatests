<Window x:Class="Yatests.FolderWindow.MBFolder.Staff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Yatests.FolderWindow.MBFolder"
        mc:Ignorable="d"
        Title="Описание" 
        Height="171" 
        Width="455" 
        ResizeMode="NoResize" 
        WindowStyle="None" 
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown" Topmost="True"        
        Background="Transparent"
        AllowsTransparency="True">
    <Border Width="Auto" Height="Auto"
          BorderThickness="2"
          CornerRadius="10">
        <Border.Background>
            <ImageBrush ImageSource="/Yatests;component/FolderResource/FolderImage/Background.jpg" Stretch="None"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="34"/>
                <RowDefinition Height="10"/>
            </Grid.RowDefinitions>
            <TextBlock 
            Style="{DynamicResource T1}"
            HorizontalAlignment="Center" 
            Margin="155,11,152,0" 
            Text="Роль сотрудника" 
            VerticalAlignment="Top" Width="144"
            />
            <TextBlock x:Name="Er"  Margin="15,59,0,0" 
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   TextWrapping="Wrap" 
                   Style="{DynamicResource T2}"
                   Text="В этой роли вы можете: проходить теты и смотреть свои результаты"
                   TextAlignment="Center" Width="426"/>
            <Image  x:Name="BTClose"
                HorizontalAlignment="Right"
                Source="/FolderResource/FolderImage/X.png"
                MouseLeftButtonDown="CloseWindow"
                Height="13"
                Width="13" Margin="0,10,10,100" Cursor="Hand"/>
            <Image  x:Name="BTOk"
                Source="/FolderResource/FolderImage/Ok.png"
                MouseLeftButtonDown="CloseWindow"
                Grid.Row="1" Cursor="Hand"/>
            <Image  x:Name="BMistake"
                HorizontalAlignment="Left"
                Source="/FolderResource/FolderImage/Question.png"
                Height="18"
                Width="18" Margin="10,8,0,97"/>
        </Grid>
    </Border>
</Window>
